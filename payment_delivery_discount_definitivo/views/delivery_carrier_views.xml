<odoo>
  <record id="delivery_carrier_form_inherit_discount" model="ir.ui.view">
    <field name="name">delivery.carrier.form.discount</field>
    <field name="model">delivery.carrier</field>
    <!-- Usamos el XML ID real de la vista core; compruébalo en Ajustes → Técnico → Vistas -->
    <field name="inherit_id" ref="delivery.view_delivery_carrier_form"/>
    <field name="arch" type="xml">
      <!-- Asegúrate de que este XPath realmente exista en la vista base -->
      <xpath expr="//group[@name='general_info']" position="inside">
        <field name="discount_percent"/>
        <field name="discount_product_id"/>
      </xpath>
    </field>
  </record>
</odoo>
